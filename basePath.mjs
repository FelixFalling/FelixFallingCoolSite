/**
 * The GitHub Pages base path — defined in ONE place.
 *
 * Imported by next.config.mjs (the build), src/data/resume.ts (project links),
 * and — via resume.ts — the Playwright tests, so the app, the static export,
 * and the tests always agree on it. If you ever rename the repo, change it here.
 */
export const BASE_PATH = "/FelixFallingCoolSite";
